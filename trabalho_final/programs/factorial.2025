def factorial(int n) {
    int result;
    int i;
    result = 1;

    for (i = 1; i < n + 1; i = i + 1) {
        result = result * i;
    }

    return result;
}

def main(int argv, string args) {
    int x;
    int y;
    int fat;
    int mdc;

    x = 6;
    y = 9;

    fat = call factorial(x);
    print fat;
}